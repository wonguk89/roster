<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.app.roster.dao.HolidayMapper">

    <select id="getAllHolidays" resultType="com.app.roster.dto.Holiday">
        SELECT * FROM holiday
    </select>

    <insert id="addHoliday" parameterType="com.app.roster.dto.Holiday">
        INSERT INTO holiday (holidayDate, description, workforce)
        VALUES (#{holidayDate}, #{description}, #{workforce})
    </insert>

    <update id="updateHoliday" parameterType="com.app.roster.dto.Holiday">
        UPDATE holiday
        SET holidayDate = #{holidayDate}, description = #{description}, workforce = #{workforce}
        WHERE holidayID = #{holidayID}
    </update>

    <delete id="deleteHoliday" parameterType="com.app.roster.dto.Holiday">
        DELETE FROM holiday WHERE holidayID = #{holidayID}
    </delete>

</mapper>